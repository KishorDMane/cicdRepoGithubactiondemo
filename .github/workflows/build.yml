on : push
jobs : 
    first-job:
      runs-on : windows-latest
      steps :
         - run : node --version
         - run : npm --version
         - user : actions/checkout@v3
         - run : npm i
         - run : node index.js

