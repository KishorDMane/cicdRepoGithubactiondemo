on : push
jobs : 
    first-job:
      runs-on : windows-latest
      steps :
         - run : node --version
         - run : nom --version
         - user : actions/checkout@v3
         -run : index.js
         